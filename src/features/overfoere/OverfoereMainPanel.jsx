import React from 'react';
import TransferFormContainer from './OverfoereFormContainer';

const mainPanel = () => (
  <main role="main" className="l-transfer__form">
    <TransferFormContainer />
  </main>
);

export default mainPanel;
